apiVersion: v1
kind: Service
metadata:
  name: devops-azure-app-service
  namespace: devops-demo
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: devops-azure-app
